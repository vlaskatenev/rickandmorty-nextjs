import React, { FC } from 'react';
// import cn from 'classnames';

const SideMenu: FC = () => {
  return (
    <nav>
      <ul>
        {
          // categories.map(menuItem => (
          //   <div></div>
          // ))
        }
      </ul>
    </nav>
  );
};

export default SideMenu;
